server {
    listen {{ ctfd_port }};
    server_name _;

    # Static OSINT challenge assets
    location /osint/ {
        alias {{ osint_static_web_root }}/;
        autoindex off;
        try_files $uri $uri/ =404;
    }

    # Static downloadable files (forensics, reverse)
    location /files/ {
        alias /var/www/ctf/files/;
        autoindex off;
        add_header Content-Disposition 'attachment';
        try_files $uri =404;
    }

    # CTFd reverse proxy
    location / {
        proxy_pass http://127.0.0.1:{{ ctfd_internal_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        client_max_body_size 50M;
    }
}
